// Updated storage rules to allow airline_updates path
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /airline_updates/{allPaths=**} {
      allow read: if true; // In prod, restrict this to authenticated users or admins
      allow write: if request.auth != null;
    }
  }
}
