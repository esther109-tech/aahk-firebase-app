// Updated storage rules to allow airline_updates path
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /airline_updates/{allPaths=**} {
      allow read: if request.auth != null; // Restricted to authenticated users in production
      allow write: if request.auth != null;
    }
  }
}
